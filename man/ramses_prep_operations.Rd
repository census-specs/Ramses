% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data_prep.R
\name{ramses_prep_diagnose}
\alias{ramses_prep_diagnose}
\alias{ramses_prep_rename}
\alias{ramses_prep_drop}
\alias{ramses_prep_reorder}
\alias{ramses_prep_select_vars}
\alias{ramses_prep_cast}
\alias{ramses_prep_recode}
\alias{ramses_prep_replace_value}
\alias{ramses_prep_compute}
\alias{ramses_prep_impute_na}
\alias{ramses_prep_deduplicate}
\alias{ramses_prep_trim_ws}
\alias{ramses_prep_change_case}
\alias{ramses_prep_filter}
\alias{ramses_prep_sort}
\alias{ramses_prep_apply_step}
\alias{ramses_prep_apply_pipeline}
\title{Fonctions utilitaires et securisees de preparation et nettoyage des donnees}
\usage{
ramses_prep_diagnose(df)

ramses_prep_rename(df, old_name, new_name)

ramses_prep_drop(df, vars_to_drop)

ramses_prep_reorder(df, priority_vars)

ramses_prep_select_vars(df, vars_to_keep)

ramses_prep_cast(df, var_name, target_type, date_format = "\%Y-\%m-\%d")

ramses_prep_recode(df, var_name, recode_map, default_action = "keep")

ramses_prep_replace_value(df, var_name, old_value, new_value, is_old_na = FALSE, is_new_na = FALSE)

ramses_prep_compute(df, new_var_name, expr_str)

ramses_prep_impute_na(df, vars, method = c("drop_rows", "fixed", "mean", "median", "mode"), fixed_value = "0")

ramses_prep_deduplicate(df, vars = NULL, keep = c("first", "last"))

ramses_prep_trim_ws(df, vars = NULL, mode = c("both", "squish", "left", "right"))

ramses_prep_change_case(df, vars = NULL, target_case = c("lower", "upper", "title", "sentence"))

ramses_prep_filter(df, conditions, combine_op = c("AND", "OR"))

ramses_prep_sort(df, sort_vars, decreasing = FALSE)

ramses_prep_apply_step(df, step)

ramses_prep_apply_pipeline(df, pipeline)
}
\arguments{
\item{df}{Data.frame d'entree.}
\item{old_name}{Nom actuel de la colonne.}
\item{new_name}{Nouveau nom de la colonne.}
\item{vars_to_drop}{Vecteur de noms de colonnes a supprimer.}
\item{priority_vars}{Vecteur de noms de colonnes a placer en debut de data.frame.}
\item{vars_to_keep}{Vecteur de noms de colonnes a conserver.}
\item{var_name}{Nom de la variable cible.}
\item{target_type}{Type cible ("numeric", "character", "factor", "logical", "Date").}
\item{date_format}{Format de date pour les conversions Date.}
\item{recode_map}{Vecteur nomme de correspondance.}
\item{default_action}{Action pour valeurs non recodees ("keep" ou "na").}
\item{old_value}{Valeur d'origine a remplacer.}
\item{new_value}{Nouvelle valeur de remplacement.}
\item{is_old_na}{Booleen indiquant si la valeur d'origine est NA.}
\item{is_new_na}{Booleen indiquant si la nouvelle valeur est NA.}
\item{new_var_name}{Nom de la nouvelle variable calculee.}
\item{expr_str}{Chaine de calcul mathematique validee par AST.}
\item{vars}{Vecteur de noms de variables cibles.}
\item{method}{Methode d'imputation ("drop_rows", "fixed", "mean", "median", "mode").}
\item{fixed_value}{Valeur de remplacement fixe.}
\item{keep}{Occurrence a conserver ("first" ou "last").}
\item{mode}{Mode de nettoyage d'espaces ("both", "squish", "left", "right").}
\item{target_case}{Casse cible ("lower", "upper", "title", "sentence").}
\item{conditions}{Liste de conditions de filtrage.}
\item{combine_op}{Operateur logique de combinaison ("AND" ou "OR").}
\item{sort_vars}{Variables de tri.}
\item{decreasing}{Booleen ou vecteur de booleens indiquant le sens du tri.}
\item{step}{Liste decrivant une etape de transformation.}
\item{pipeline}{Liste d'etapes de transformation a appliquer successivement.}
}
\value{
Une liste contenant le data.frame transforme \code{df}, le code R reproductible \code{code} et un libelle \code{label}.
}
\description{
Ensemble de fonctions pures et securisees realisant les operations courantes
de diagnostic, nettoyage, recodage, calcul securise (AST), filtrage et tri pour Ramses.
}
